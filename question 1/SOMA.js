// Inicialização das variáveis
INDICE = 13
K = 0
SOMA = 0

// Loop for que calcula a soma dos números de 1 até o INDICE
for (let K = 1; K <= INDICE; K++) {
  SOMA += K //adiciona K ao valor de SOMA
}

// Resultado de SOMA
console.log(SOMA)
